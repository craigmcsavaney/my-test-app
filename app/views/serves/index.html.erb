<% provide(:title, 'All Serves') %>
<h1>All Serves (<%= @serves.count %>)</h1>

<div class="container">
	<%= will_paginate %>
	<ul class="class-index">
      	<%= render @serves %>
	</ul>
	<%= will_paginate %>
	<br/>
	<%= link_to 'Create New Serve', new_serve_path %><br/>
	<%= go_back %>
</div>