<li>
	<% if ["index_admin"].include?params[:action] %>
			<% @showpath = merchant_admin_path(merchant) %>
		<% else %>
			<% @showpath = merchant_path(merchant) %>
	<% end %>
	<% @a= "none chosen" %>
	<% if !merchant.button.nil? %>
		<% @a = merchant.button.name %>
	<% end %>
	<div class="row">
		<div class="col-sm-4"><%= link_to merchant.name, @showpath %></div>
		<div class="col-sm-5"><%= link_to merchant.website, @showpath %></div>
		<div class="col-sm-3"><%= link_to @a, @showpath %></div>
	</div>
</li>