<% provide(:title, 'All Users') %>
<h1>All Users (<%= @users.count %>)</h1>

<div class="container">
	<%= will_paginate %>
	<ul class="class-index">
      	<%= render @users %>
	</ul>
	<%= will_paginate %>
	<br/>
	<%= link_to 'Create New User', new_user_registration_path %><br/>
	<%= go_back %>
</div>